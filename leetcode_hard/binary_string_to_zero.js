function steps(str, k) {
    let l = str.length;
    let ans = "";
    for(let i = 0; i < str.length; i++) {
        ans += "0";
    }
    console.log(ans);
    // while(str !=)
}

steps("100010010011110", 2);